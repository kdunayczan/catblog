<h1 class="center">Welcome to <%= @user.username %>'s page</h1>

<div class="row">
	<div class="col-md-4 col-md-offset-4 center">
		<%= gravatar_for @user, size: 150 %>
	</div>
</div>
<h2 class="center"><%= pluralize(@user.posts.count, "post") %></h2>
<div align="center">
	<%= will_paginate @user_posts %>
</div>
<%= render 'posts/list', obj: @user_posts %>
<div align="center">
	<%= will_paginate @user_posts %>
</div>